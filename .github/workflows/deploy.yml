name: Simbora-Auto-Webhook
on:
  push:
    branches: [ main ]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Webhook to n8n
        run: |
          curl -X POST "https://bb20agent.app.n8n.cloud/webhook/iHXYTA7hSplRzwb9LS3tt" \
          -H "Content-Type: application/json" \
          -d "{\"event\":\"push\", \"repo\":\"antigravity-bot\"}"