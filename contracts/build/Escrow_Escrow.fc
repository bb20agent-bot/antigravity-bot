#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; Escrow_Escrow.headers.fc
;;
;; Header files for Escrow
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_slice_eq
int __tact_slice_eq(slice a, slice b) inline;

;; __tact_dict_get_int_cell
cell __tact_dict_get_int_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_int_cell
(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline;

;; $DeployOk$_store
builder $DeployOk$_store(builder build_0, (int) v) inline;

;; $DeployOk$_store_cell
cell $DeployOk$_store_cell((int) v, builder b) inline;

;; $JettonTransfer$_store
builder $JettonTransfer$_store(builder build_0, (int, int, slice, slice, cell, int, slice) v) inline;

;; $JettonTransfer$_store_cell
cell $JettonTransfer$_store_cell((int, int, slice, slice, cell, int, slice) v, builder b) inline;

;; $TradeDetails$_store
builder $TradeDetails$_store(builder build_0, (slice, slice, int, int, int, int, slice, int, int) v) inline;

;; $TradeDetails$_store_cell
cell $TradeDetails$_store_cell((slice, slice, int, int, int, int, slice, int, int) v, builder b) inline;

;; $TradeDetails$_load
(slice, ((slice, slice, int, int, int, int, slice, int, int))) $TradeDetails$_load(slice sc_0) inline;

;; $TradeDetails$_as_optional
tuple $TradeDetails$_as_optional((slice, slice, int, int, int, int, slice, int, int) v) inline;

;; $TradeDetails$_load_opt
tuple $TradeDetails$_load_opt(cell cl) inline;

;; $Escrow$_store
builder $Escrow$_store(builder build_0, (slice, slice, slice, cell, int) v) inline;

;; $Escrow$_load
(slice, ((slice, slice, slice, cell, int))) $Escrow$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $TradeDetails$_not_null
((slice, slice, int, int, int, int, slice, int, int)) $TradeDetails$_not_null(tuple v) inline;

;; $Escrow$init$_load
(slice, ((slice, slice, slice))) $Escrow$init$_load(slice sc_0) inline;

;; $Escrow$_contract_init
(slice, slice, slice, cell, int) $Escrow$_contract_init(slice $owner, slice $treasuryAddress, slice $voraTokenAddress) impure inline;

;; $Escrow$_contract_load
(slice, slice, slice, cell, int) $Escrow$_contract_load() impure inline;

;; $Escrow$_contract_store
() $Escrow$_contract_store((slice, slice, slice, cell, int) v) impure inline;

;; $MessageParameters$_constructor_bounce_to_value_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline;

;; $Escrow$_fun_notify
((slice, slice, slice, cell, int), ()) $Escrow$_fun_notify((slice, slice, slice, cell, int) $self, cell $body) impure inline;

;; $TradeDetails$_constructor_buyer_seller_tonAmount_voraAmount_feePercentage_reBatePercentage_status_createdAt_timeoutLength
((slice, slice, int, int, int, int, slice, int, int)) $TradeDetails$_constructor_buyer_seller_tonAmount_voraAmount_feePercentage_reBatePercentage_status_createdAt_timeoutLength(slice $buyer, slice $seller, int $tonAmount, int $voraAmount, int $feePercentage, int $reBatePercentage, slice $status, int $createdAt, int $timeoutLength) inline;

;; $SendParameters$_constructor_to_value_mode
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode(slice $to, int $value, int $mode) inline;

;; $SendParameters$_constructor_to_value_mode_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline;

;; $JettonTransfer$_constructor_queryId_amount_destination_responseDestination_customPayload_forwardTonAmount_forwardPayload
((int, int, slice, slice, cell, int, slice)) $JettonTransfer$_constructor_queryId_amount_destination_responseDestination_customPayload_forwardTonAmount_forwardPayload(int $queryId, int $amount, slice $destination, slice $responseDestination, cell $customPayload, int $forwardTonAmount, slice $forwardPayload) inline;

;; $DeployOk$_constructor_queryId
((int)) $DeployOk$_constructor_queryId(int $queryId) inline;


;; Escrow_Escrow.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

int __tact_slice_eq(slice a, slice b) inline {
    return (a.slice_hash() ==  b.slice_hash());
}

forall X0, X1, X2, X3, X4, X5, X6, X7, X8 -> tuple __tact_tuple_create_9((X0, X1, X2, X3, X4, X5, X6, X7, X8) v) asm """
    9 TUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7, X8 -> (X0, X1, X2, X3, X4, X5, X6, X7, X8) __tact_tuple_destroy_9(tuple v) asm """
    9 UNTUPLE
""";

cell __tact_dict_get_int_cell(cell d, int kl, int k) inline {
    var (r, ok) = idict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_ref(d, kl, k, v), ());
    }
}

slice $global_emptySlice() impure asm """
    b{} PUSHSLICE
""";

int $global_myBalance() impure asm """
    BALANCE FIRST
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

int $global_now() impure asm """
    NOW
""";

((slice, slice, slice, cell, int), ()) $Escrow$_fun_notify((slice, slice, slice, cell, int) $self, cell $body) impure inline {
    var (($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter)) = $self;
    slice $to = __tact_context_get_sender();
    int $bounce = false;
    if (false) {
        int $balance = $global_myBalance();
        int $balanceBeforeMessage = ($balance - $Context$_get_value(__tact_context_get()));
        if (($balanceBeforeMessage < 0)) {
            $global_nativeReserve(0, 0);
            $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 130, $body));
            return (($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter), ());
        }
    }
    $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 66, $body));
    return (($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter), ());
}

;; Escrow_Escrow.constants.fc
;; String "LOCKED"
slice __gen_slice_string_1bbc70525820685017d4dae22fdd79ac5fe2cf06f11195d584eee325654b8f9d() asm """
    B{b5ee9c7241010101000800000c4c4f434b4544bdd33993} B>boc <s PUSHSLICE
""";

;; String "COMPLETED"
slice __gen_slice_string_3ede81f299c6eccf831090c6d4643c351dfce36d368c2858a097b82cd562607d() asm """
    B{b5ee9c7241010101000b000012434f4d504c4554454428b062e9} B>boc <s PUSHSLICE
""";

;; String "CANCELLED"
slice __gen_slice_string_c50789ffab5422a0771499eb1ee11f8872d88960fb4dcf7527d4f9c439084c17() asm """
    B{b5ee9c7241010101000b00001243414e43454c4c454441ac0dca} B>boc <s PUSHSLICE
""";

;; Escrow_Escrow.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode(slice $to, int $value, int $mode) inline {
    return ($mode, null(), null(), null(), $value, $to, true);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, true);
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: DeployOk
;; Header: 0xaff90f57
;; TLB: deploy_ok#aff90f57 queryId:uint64 = DeployOk
;;

builder $DeployOk$_store(builder build_0, (int) v) inline {
    var (v'queryId) = v;
    build_0 = store_uint(build_0, 2952335191, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    return build_0;
}

cell $DeployOk$_store_cell((int) v, builder b) inline {
    return $DeployOk$_store(b, v).end_cell();
}

((int)) $DeployOk$_constructor_queryId(int $queryId) inline {
    return ($queryId);
}

;;
;; Type: JettonTransfer
;; Header: 0x0f8a7ea5
;; TLB: jetton_transfer#0f8a7ea5 queryId:uint64 amount:coins destination:address responseDestination:address customPayload:Maybe ^cell forwardTonAmount:coins forwardPayload:remainder<slice> = JettonTransfer
;;

builder $JettonTransfer$_store(builder build_0, (int, int, slice, slice, cell, int, slice) v) inline {
    var (v'queryId, v'amount, v'destination, v'responseDestination, v'customPayload, v'forwardTonAmount, v'forwardPayload) = v;
    build_0 = store_uint(build_0, 260734629, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    build_0 = build_0.store_varuint16(v'amount);
    build_0 = build_0.store_slice(v'destination);
    build_0 = build_0.store_slice(v'responseDestination);
    build_0 = build_0.store_maybe_ref(v'customPayload);
    build_0 = build_0.store_varuint16(v'forwardTonAmount);
    build_0 = build_0.store_slice(v'forwardPayload);
    return build_0;
}

cell $JettonTransfer$_store_cell((int, int, slice, slice, cell, int, slice) v, builder b) inline {
    return $JettonTransfer$_store(b, v).end_cell();
}

((int, int, slice, slice, cell, int, slice)) $JettonTransfer$_constructor_queryId_amount_destination_responseDestination_customPayload_forwardTonAmount_forwardPayload(int $queryId, int $amount, slice $destination, slice $responseDestination, cell $customPayload, int $forwardTonAmount, slice $forwardPayload) inline {
    return ($queryId, $amount, $destination, $responseDestination, $customPayload, $forwardTonAmount, $forwardPayload);
}

;;
;; Type: TradeDetails
;; TLB: _ buyer:address seller:address tonAmount:int257 voraAmount:int257 feePercentage:int257 reBatePercentage:int257 status:^string createdAt:int257 timeoutLength:int257 = TradeDetails
;;

builder $TradeDetails$_store(builder build_0, (slice, slice, int, int, int, int, slice, int, int) v) inline {
    var (v'buyer, v'seller, v'tonAmount, v'voraAmount, v'feePercentage, v'reBatePercentage, v'status, v'createdAt, v'timeoutLength) = v;
    build_0 = build_0.store_slice(v'buyer);
    build_0 = build_0.store_slice(v'seller);
    build_0 = build_0.store_int(v'tonAmount, 257);
    var build_1 = begin_cell();
    build_1 = build_1.store_int(v'voraAmount, 257);
    build_1 = build_1.store_int(v'feePercentage, 257);
    build_1 = build_1.store_int(v'reBatePercentage, 257);
    build_1 = build_1.store_builder_ref(begin_cell().store_slice(v'status));
    var build_2 = begin_cell();
    build_2 = build_2.store_int(v'createdAt, 257);
    build_2 = build_2.store_int(v'timeoutLength, 257);
    build_1 = store_builder_ref(build_1, build_2);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $TradeDetails$_store_cell((slice, slice, int, int, int, int, slice, int, int) v, builder b) inline {
    return $TradeDetails$_store(b, v).end_cell();
}

(slice, ((slice, slice, int, int, int, int, slice, int, int))) $TradeDetails$_load(slice sc_0) inline {
    var v'buyer = sc_0~load_msg_addr();
    var v'seller = sc_0~load_msg_addr();
    var v'tonAmount = sc_0~load_int(257);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'voraAmount = sc_1~load_int(257);
    var v'feePercentage = sc_1~load_int(257);
    var v'reBatePercentage = sc_1~load_int(257);
    var v'status = sc_1~load_ref().begin_parse();
    slice sc_2 = sc_1~load_ref().begin_parse();
    var v'createdAt = sc_2~load_int(257);
    var v'timeoutLength = sc_2~load_int(257);
    return (sc_0, (v'buyer, v'seller, v'tonAmount, v'voraAmount, v'feePercentage, v'reBatePercentage, v'status, v'createdAt, v'timeoutLength));
}

tuple $TradeDetails$_as_optional((slice, slice, int, int, int, int, slice, int, int) v) inline {
    var (v'buyer, v'seller, v'tonAmount, v'voraAmount, v'feePercentage, v'reBatePercentage, v'status, v'createdAt, v'timeoutLength) = v;
    return __tact_tuple_create_9(v'buyer, v'seller, v'tonAmount, v'voraAmount, v'feePercentage, v'reBatePercentage, v'status, v'createdAt, v'timeoutLength);
}

tuple $TradeDetails$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $TradeDetails$_as_optional(sc~$TradeDetails$_load());
}

((slice, slice, int, int, int, int, slice, int, int)) $TradeDetails$_not_null(tuple v) inline {
    throw_if(128, null?(v));
    var (slice vvv'buyer, slice vvv'seller, int vvv'tonAmount, int vvv'voraAmount, int vvv'feePercentage, int vvv'reBatePercentage, slice vvv'status, int vvv'createdAt, int vvv'timeoutLength) = __tact_tuple_destroy_9(v);
    return (vvv'buyer, vvv'seller, vvv'tonAmount, vvv'voraAmount, vvv'feePercentage, vvv'reBatePercentage, vvv'status, vvv'createdAt, vvv'timeoutLength);
}

((slice, slice, int, int, int, int, slice, int, int)) $TradeDetails$_constructor_buyer_seller_tonAmount_voraAmount_feePercentage_reBatePercentage_status_createdAt_timeoutLength(slice $buyer, slice $seller, int $tonAmount, int $voraAmount, int $feePercentage, int $reBatePercentage, slice $status, int $createdAt, int $timeoutLength) inline {
    return ($buyer, $seller, $tonAmount, $voraAmount, $feePercentage, $reBatePercentage, $status, $createdAt, $timeoutLength);
}

;;
;; Type: Escrow
;; TLB: _ owner:address treasuryAddress:address voraTokenAddress:address trades:dict<int, ^TradeDetails{buyer:address,seller:address,tonAmount:int257,voraAmount:int257,feePercentage:int257,reBatePercentage:int257,status:^string,createdAt:int257,timeoutLength:int257}> tradeCounter:uint64 = Escrow
;;

builder $Escrow$_store(builder build_0, (slice, slice, slice, cell, int) v) inline {
    var (v'owner, v'treasuryAddress, v'voraTokenAddress, v'trades, v'tradeCounter) = v;
    build_0 = build_0.store_slice(v'owner);
    build_0 = build_0.store_slice(v'treasuryAddress);
    build_0 = build_0.store_slice(v'voraTokenAddress);
    build_0 = build_0.store_dict(v'trades);
    build_0 = build_0.store_uint(v'tradeCounter, 64);
    return build_0;
}

(slice, ((slice, slice, slice, cell, int))) $Escrow$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'treasuryAddress = sc_0~load_msg_addr();
    var v'voraTokenAddress = sc_0~load_msg_addr();
    var v'trades = sc_0~load_dict();
    var v'tradeCounter = sc_0~load_uint(64);
    return (sc_0, (v'owner, v'treasuryAddress, v'voraTokenAddress, v'trades, v'tradeCounter));
}

(slice, ((slice, slice, slice))) $Escrow$init$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'treasuryAddress = sc_0~load_msg_addr();
    var v'voraTokenAddress = sc_0~load_msg_addr();
    return (sc_0, (v'owner, v'treasuryAddress, v'voraTokenAddress));
}

(slice, slice, slice, cell, int) $Escrow$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$Escrow$_load();
    }
    else {
        (slice $owner, slice $treasuryAddress, slice $voraTokenAddress) = $sc~$Escrow$init$_load();
        $sc.end_parse();
        return $Escrow$_contract_init($owner, $treasuryAddress, $voraTokenAddress);
    }
}

() $Escrow$_contract_store((slice, slice, slice, cell, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $Escrow$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract Escrow functions
;;

(slice, slice, slice, cell, int) $Escrow$_contract_init(slice $owner, slice $treasuryAddress, slice $voraTokenAddress) impure inline {
    var (($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter)) = (null(), null(), null(), null(), 0);
    $self'owner = $owner;
    $self'treasuryAddress = $treasuryAddress;
    $self'voraTokenAddress = $voraTokenAddress;
    return ($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter);
}

;;
;; Get methods of a Contract Escrow
;;

;;
;; Routing of a Contract Escrow
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter) = $Escrow$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    var op = in_msg~load_opcode_internal();
    ;; Receive CreateTrade message
    if (op == 2495137400) {
        var $msg'buyer = in_msg~load_msg_addr();
        var $msg'seller = in_msg~load_msg_addr();
        var $msg'tonAmount = in_msg~load_int(257);
        slice sc_1 = in_msg~load_ref().begin_parse();
        var $msg'voraAmount = sc_1~load_int(257);
        var $msg'feePercentage = sc_1~load_int(257);
        var $msg'reBatePercentage = sc_1~load_int(257);
        slice sc_2 = sc_1~load_ref().begin_parse();
        var $msg'timeoutLength = sc_2~load_int(257);
        int $tradeId = $self'tradeCounter;
        $self'trades~__tact_dict_set_int_cell(257, $tradeId, $TradeDetails$_store_cell($TradeDetails$_constructor_buyer_seller_tonAmount_voraAmount_feePercentage_reBatePercentage_status_createdAt_timeoutLength($msg'buyer, $msg'seller, $msg'tonAmount, $msg'voraAmount, $msg'feePercentage, $msg'reBatePercentage, __gen_slice_string_1bbc70525820685017d4dae22fdd79ac5fe2cf06f11195d584eee325654b8f9d(), $global_now(), $msg'timeoutLength), begin_cell()));
        $self'tradeCounter = ($self'tradeCounter + 1);
        $Escrow$_contract_store(($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter));
        return ();
    }
    
    ;; Receive ReleaseTrade message
    if (op == 3100649420) {
        var $msg'tradeId = in_msg~load_int(257);
        var ($trade'buyer, $trade'seller, $trade'tonAmount, $trade'voraAmount, $trade'feePercentage, $trade'reBatePercentage, $trade'status, $trade'createdAt, $trade'timeoutLength) = $TradeDetails$_not_null($TradeDetails$_load_opt(__tact_dict_get_int_cell($self'trades, 257, $msg'tradeId)));
        throw_unless(38237, __tact_slice_eq($trade'status, __gen_slice_string_1bbc70525820685017d4dae22fdd79ac5fe2cf06f11195d584eee325654b8f9d()));
        int $totalFee = (($trade'tonAmount * $trade'feePercentage) / 1000);
        int $treasuryFee = (($totalFee * 30) / 100);
        int $sellerFee = (($totalFee * 40) / 100);
        int $rebate = (($totalFee * $trade'reBatePercentage) / 100);
        int $devFund = ((($totalFee - $treasuryFee) - $sellerFee) - $rebate);
        int $sellerEarnings = (($trade'tonAmount - $totalFee) + $sellerFee);
        $global_send($SendParameters$_constructor_to_value_mode($trade'seller, $sellerEarnings, 2));
        $global_send($SendParameters$_constructor_to_value_mode_body($self'voraTokenAddress, 50000000, 2, $JettonTransfer$_store_cell($JettonTransfer$_constructor_queryId_amount_destination_responseDestination_customPayload_forwardTonAmount_forwardPayload(0, $trade'voraAmount, $trade'buyer, $trade'buyer, null(), 0, $global_emptySlice()), begin_cell())));
        $global_send($SendParameters$_constructor_to_value_mode($trade'buyer, $rebate, 2));
        $global_send($SendParameters$_constructor_to_value_mode($self'treasuryAddress, $treasuryFee, 2));
        $trade'status = __gen_slice_string_3ede81f299c6eccf831090c6d4643c351dfce36d368c2858a097b82cd562607d();
        $self'trades~__tact_dict_set_int_cell(257, $msg'tradeId, $TradeDetails$_store_cell(($trade'buyer, $trade'seller, $trade'tonAmount, $trade'voraAmount, $trade'feePercentage, $trade'reBatePercentage, $trade'status, $trade'createdAt, $trade'timeoutLength), begin_cell()));
        $Escrow$_contract_store(($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter));
        return ();
    }
    
    ;; Receive CancelTrade message
    if (op == 3059028097) {
        var $msg'tradeId = in_msg~load_int(257);
        var ($trade'buyer, $trade'seller, $trade'tonAmount, $trade'voraAmount, $trade'feePercentage, $trade'reBatePercentage, $trade'status, $trade'createdAt, $trade'timeoutLength) = $TradeDetails$_not_null($TradeDetails$_load_opt(__tact_dict_get_int_cell($self'trades, 257, $msg'tradeId)));
        throw_unless(38237, __tact_slice_eq($trade'status, __gen_slice_string_1bbc70525820685017d4dae22fdd79ac5fe2cf06f11195d584eee325654b8f9d()));
        throw_unless(40083, ($global_now() > ($trade'createdAt + $trade'timeoutLength)));
        $global_send($SendParameters$_constructor_to_value_mode($trade'buyer, $trade'tonAmount, 2));
        $global_send($SendParameters$_constructor_to_value_mode_body($self'voraTokenAddress, 50000000, 2, $JettonTransfer$_store_cell($JettonTransfer$_constructor_queryId_amount_destination_responseDestination_customPayload_forwardTonAmount_forwardPayload(0, $trade'voraAmount, $trade'seller, $trade'seller, null(), 0, $global_emptySlice()), begin_cell())));
        $trade'status = __gen_slice_string_c50789ffab5422a0771499eb1ee11f8872d88960fb4dcf7527d4f9c439084c17();
        $self'trades~__tact_dict_set_int_cell(257, $msg'tradeId, $TradeDetails$_store_cell(($trade'buyer, $trade'seller, $trade'tonAmount, $trade'voraAmount, $trade'feePercentage, $trade'reBatePercentage, $trade'status, $trade'createdAt, $trade'timeoutLength), begin_cell()));
        $Escrow$_contract_store(($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter));
        return ();
    }
    
    ;; Receive Deploy message
    if (op == 0x946a98b6) {
        var $deploy'queryId = in_msg~load_uint(64);
        ($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter)~$Escrow$_fun_notify($DeployOk$_store_cell($DeployOk$_constructor_queryId($deploy'queryId), begin_cell()));
        $Escrow$_contract_store(($self'owner, $self'treasuryAddress, $self'voraTokenAddress, $self'trades, $self'tradeCounter));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}