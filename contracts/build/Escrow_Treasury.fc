#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; Escrow_Treasury.headers.fc
;;
;; Header files for Treasury
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_store_address_opt
builder __tact_store_address_opt(builder b, slice address) inline;

;; __tact_not_null
forall X -> X __tact_not_null(X x) impure inline;

;; __tact_dump_int
() __tact_dump_int(int number, slice debug_print_1, slice debug_print_2) impure inline_ref;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_slice_eq_bits_nullable_right
int __tact_slice_eq_bits_nullable_right(slice a, slice b) inline;

;; $DeployOk$_store
builder $DeployOk$_store(builder build_0, (int) v) inline;

;; $DeployOk$_store_cell
cell $DeployOk$_store_cell((int) v, builder b) inline;

;; $TreasuryBalanceResponse$_store
builder $TreasuryBalanceResponse$_store(builder build_0, (int, int, slice, int) v) inline;

;; $TreasuryBalanceResponse$_store_cell
cell $TreasuryBalanceResponse$_store_cell((int, int, slice, int) v, builder b) inline;

;; $Treasury$_store
builder $Treasury$_store(builder build_0, (slice, slice, slice) v) inline;

;; $Treasury$_load
(slice, ((slice, slice, slice))) $Treasury$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $Treasury$init$_load
(slice, ((slice))) $Treasury$init$_load(slice sc_0) inline;

;; $Treasury$_contract_init
(slice, slice, slice) $Treasury$_contract_init(slice $owner) impure inline;

;; $Treasury$_contract_load
(slice, slice, slice) $Treasury$_contract_load() impure inline;

;; $Treasury$_contract_store
() $Treasury$_contract_store((slice, slice, slice) v) impure inline;

;; $Treasury$_fun_get_balance
((slice, slice, slice), int) $Treasury$_fun_get_balance((slice, slice, slice) $self) impure inline_ref;

;; $MessageParameters$_constructor_bounce_to_value_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline;

;; $Treasury$_fun_notify
((slice, slice, slice), ()) $Treasury$_fun_notify((slice, slice, slice) $self, cell $body) impure inline;

;; $SendParameters$_constructor_to_bounce_value_mode
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_bounce_value_mode(slice $to, int $bounce, int $value, int $mode) inline;

;; $SendParameters$_constructor_to_value_mode_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline;

;; $TreasuryBalanceResponse$_constructor_queryId_mintAmount_receiver_treasuryBalance
((int, int, slice, int)) $TreasuryBalanceResponse$_constructor_queryId_mintAmount_receiver_treasuryBalance(int $queryId, int $mintAmount, slice $receiver, int $treasuryBalance) inline;

;; $DeployOk$_constructor_queryId
((int)) $DeployOk$_constructor_queryId(int $queryId) inline;


;; Escrow_Treasury.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(slice, slice) __tact_load_address_opt(slice cs) asm """

            b{00} SDBEGINSQ
            IF:<{
              PUSHNULL
            }>ELSE<{
              LDMSGADDR
              SWAP
            }>

""";

builder __tact_store_addr_none(builder b) asm "b{00} STSLICECONST";

builder __tact_store_address_opt(builder b, slice address) inline {
    if (null?(address)) {
        return __tact_store_addr_none(b);
    } else {
        return b.store_slice(address);
    }
}

forall X -> X __tact_not_null(X x) impure inline {
    throw_if(128, null?(x)); return x;
}

() __tact_dump_str(slice value, slice debug_print_1, slice debug_print_2) impure asm """
    STRDUMP DROP STRDUMP DROP STRDUMP DROP
""";

slice $Int$_fun_toString(int $self) impure asm """
    <{
        // x
        NEWC // x b
        OVER // x b x
        0 LESSINT // x b <0?
        <{
            // x b
            45 PUSHINT // x b 45
            SWAP // x 45 b
            8 STU // x b
            SWAP // b x
            NEGATE // b -x
            SWAP // -x b
        }>CONT IF
        // x b

        SWAP // b x

        <{
            // b x
            10 PUSHINT DIVMOD // b x/10 x%10
            48 ADDCONST // b x/10 (x%10+48)
            s2 s2 s0 XC2PU ISZERO // (x%10+48) b x/10 x/10==0?
        }>CONT UNTIL
        // ... b x

        DROP // ... b
        DEPTH DEC // ... b n
        <{ 8 STU }>CONT REPEAT // b
    }>CONT 1 1 CALLXARGS
    // b

    ENDC CTOS // s
""";

() __tact_dump_int(int number, slice debug_print_1, slice debug_print_2) impure inline_ref {
    if (null?(number)) {
        __tact_dump_str("null", debug_print_1, debug_print_2);
    } else {
        __tact_dump_str($Int$_fun_toString(number), debug_print_1, debug_print_2);
    }
}

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

int __tact_slice_eq_bits_nullable_right(slice a, slice b) inline {
    return (null?(b)) ? (false) : (equal_slices_bits(a, b));
}

int $global_myBalance() impure asm """
    BALANCE FIRST
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

((slice, slice, slice), ()) $Treasury$_fun_notify((slice, slice, slice) $self, cell $body) impure inline {
    var (($self'owner, $self'voraTokenAddress, $self'botWalletAddress)) = $self;
    slice $to = __tact_context_get_sender();
    int $bounce = false;
    if (false) {
        int $balance = $global_myBalance();
        int $balanceBeforeMessage = ($balance - $Context$_get_value(__tact_context_get()));
        if (($balanceBeforeMessage < 0)) {
            $global_nativeReserve(0, 0);
            $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 130, $body));
            return (($self'owner, $self'voraTokenAddress, $self'botWalletAddress), ());
        }
    }
    $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 66, $body));
    return (($self'owner, $self'voraTokenAddress, $self'botWalletAddress), ());
}

;; Escrow_Treasury.constants.fc
;; String "dump(context().value)"
slice __gen_slice_string_c25657de7f608ae0453d544683e492429492a857961aeea8697a8226642f9787() asm """
    B{b5ee9c7241010101001700002a64756d7028636f6e7465787428292e76616c75652957da40a0} B>boc <s PUSHSLICE
""";

;; Escrow_Treasury.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_bounce_value_mode(slice $to, int $bounce, int $value, int $mode) inline {
    return ($mode, null(), null(), null(), $value, $to, $bounce);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, true);
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: DeployOk
;; Header: 0xaff90f57
;; TLB: deploy_ok#aff90f57 queryId:uint64 = DeployOk
;;

builder $DeployOk$_store(builder build_0, (int) v) inline {
    var (v'queryId) = v;
    build_0 = store_uint(build_0, 2952335191, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    return build_0;
}

cell $DeployOk$_store_cell((int) v, builder b) inline {
    return $DeployOk$_store(b, v).end_cell();
}

((int)) $DeployOk$_constructor_queryId(int $queryId) inline {
    return ($queryId);
}

;;
;; Type: TreasuryBalanceResponse
;; Header: 0x894f25e2
;; TLB: treasury_balance_response#894f25e2 queryId:int257 mintAmount:int257 receiver:address treasuryBalance:int257 = TreasuryBalanceResponse
;;

builder $TreasuryBalanceResponse$_store(builder build_0, (int, int, slice, int) v) inline {
    var (v'queryId, v'mintAmount, v'receiver, v'treasuryBalance) = v;
    build_0 = store_uint(build_0, 2303665634, 32);
    build_0 = build_0.store_int(v'queryId, 257);
    build_0 = build_0.store_int(v'mintAmount, 257);
    build_0 = build_0.store_slice(v'receiver);
    var build_1 = begin_cell();
    build_1 = build_1.store_int(v'treasuryBalance, 257);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $TreasuryBalanceResponse$_store_cell((int, int, slice, int) v, builder b) inline {
    return $TreasuryBalanceResponse$_store(b, v).end_cell();
}

((int, int, slice, int)) $TreasuryBalanceResponse$_constructor_queryId_mintAmount_receiver_treasuryBalance(int $queryId, int $mintAmount, slice $receiver, int $treasuryBalance) inline {
    return ($queryId, $mintAmount, $receiver, $treasuryBalance);
}

;;
;; Type: Treasury
;; TLB: _ owner:address voraTokenAddress:address botWalletAddress:address = Treasury
;;

builder $Treasury$_store(builder build_0, (slice, slice, slice) v) inline {
    var (v'owner, v'voraTokenAddress, v'botWalletAddress) = v;
    build_0 = build_0.store_slice(v'owner);
    build_0 = __tact_store_address_opt(build_0, v'voraTokenAddress);
    build_0 = __tact_store_address_opt(build_0, v'botWalletAddress);
    return build_0;
}

(slice, ((slice, slice, slice))) $Treasury$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'voraTokenAddress = sc_0~__tact_load_address_opt();
    var v'botWalletAddress = sc_0~__tact_load_address_opt();
    return (sc_0, (v'owner, v'voraTokenAddress, v'botWalletAddress));
}

(slice, ((slice))) $Treasury$init$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    return (sc_0, (v'owner));
}

(slice, slice, slice) $Treasury$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$Treasury$_load();
    }
    else {
        (slice $owner) = $sc~$Treasury$init$_load();
        $sc.end_parse();
        return $Treasury$_contract_init($owner);
    }
}

() $Treasury$_contract_store((slice, slice, slice) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $Treasury$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract Treasury functions
;;

(slice, slice, slice) $Treasury$_contract_init(slice $owner) impure inline {
    var (($self'owner, $self'voraTokenAddress, $self'botWalletAddress)) = (null(), null(), null());
    $self'owner = $owner;
    $self'voraTokenAddress = null();
    $self'botWalletAddress = null();
    return ($self'owner, $self'voraTokenAddress, $self'botWalletAddress);
}

((slice, slice, slice), int) $Treasury$_fun_get_balance((slice, slice, slice) $self) impure inline_ref {
    var (($self'owner, $self'voraTokenAddress, $self'botWalletAddress)) = $self;
    var $fresh$ret_7 = $global_myBalance();
    return (($self'owner, $self'voraTokenAddress, $self'botWalletAddress), $fresh$ret_7);
}

;;
;; Get methods of a Contract Treasury
;;

_ %get_balance() method_id(130343) {
    var self = $Treasury$_contract_load();
    var res = self~$Treasury$_fun_get_balance();
    return res;
}

;;
;; Routing of a Contract Treasury
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'owner, $self'voraTokenAddress, $self'botWalletAddress) = $Treasury$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    int op = 0;
    int in_msg_length = slice_bits(in_msg);
    if (in_msg_length >= 32) {
        op = in_msg~load_uint(32);
        ;; Receive SetVoraTokenAddress message
        if (op == 4068122196) {
            var $msg'voraTokenAddress = in_msg~load_msg_addr();
            throw_unless(58190, ( equal_slices_bits(__tact_context_get_sender(), $self'owner) ));
            throw_unless(22230, null?($self'voraTokenAddress));
            $self'voraTokenAddress = $msg'voraTokenAddress;
            $Treasury$_contract_store(($self'owner, $self'voraTokenAddress, $self'botWalletAddress));
            return ();
        }
        
        ;; Receive SetBotWalletAddress message
        if (op == 1153750340) {
            var $msg'botWalletAddress = in_msg~load_msg_addr();
            throw_unless(58190, ( equal_slices_bits(__tact_context_get_sender(), $self'owner) ));
            $self'botWalletAddress = $msg'botWalletAddress;
            $Treasury$_contract_store(($self'owner, $self'voraTokenAddress, $self'botWalletAddress));
            return ();
        }
        
        ;; Receive BotWithdraw message
        if (op == 4213753329) {
            var $msg'amount = in_msg~load_int(257);
            var $msg'destination = in_msg~load_msg_addr();
            throw_unless(37849, (~ null?($self'botWalletAddress)));
            throw_unless(55238, ( equal_slices_bits(__tact_context_get_sender(), __tact_not_null($self'botWalletAddress)) ));
            throw_unless(54615, ($global_myBalance() >= ($msg'amount + 50000000)));
            $global_send($SendParameters$_constructor_to_bounce_value_mode($msg'destination, true, $msg'amount, 2));
            $Treasury$_contract_store(($self'owner, $self'voraTokenAddress, $self'botWalletAddress));
            return ();
        }
        
        ;; Receive TreasuryBalanceRequest message
        if (op == 1127502537) {
            var $msg'queryId = in_msg~load_int(257);
            var $msg'mintAmount = in_msg~load_int(257);
            var $msg'receiver = in_msg~load_msg_addr();
            throw_unless(14438, ( __tact_slice_eq_bits_nullable_right(__tact_context_get_sender(), $self'voraTokenAddress) ));
            $global_send($SendParameters$_constructor_to_value_mode_body(__tact_not_null($self'voraTokenAddress), 0, 64, $TreasuryBalanceResponse$_store_cell($TreasuryBalanceResponse$_constructor_queryId_mintAmount_receiver_treasuryBalance($msg'queryId, $msg'mintAmount, $msg'receiver, $global_myBalance()), begin_cell())));
            $Treasury$_contract_store(($self'owner, $self'voraTokenAddress, $self'botWalletAddress));
            return ();
        }
        
        ;; Receive Deploy message
        if (op == 0x946a98b6) {
            var $deploy'queryId = in_msg~load_uint(64);
            ($self'owner, $self'voraTokenAddress, $self'botWalletAddress)~$Treasury$_fun_notify($DeployOk$_store_cell($DeployOk$_constructor_queryId($deploy'queryId), begin_cell()));
            $Treasury$_contract_store(($self'owner, $self'voraTokenAddress, $self'botWalletAddress));
            return ();
        }
        
    }
    ;; Receive empty message
    if ((op == 0) & (in_msg_length <= 32)) {
        __tact_dump_int($Context$_get_value(__tact_context_get()), __gen_slice_string_c25657de7f608ae0453d544683e492429492a857961aeea8697a8226642f9787(), "File contracts/tact/Treasury.tact:67:9:");
        $Treasury$_contract_store(($self'owner, $self'voraTokenAddress, $self'botWalletAddress));
        return ();
    }
    ;; Empty Receiver and Text Receivers
    var text_op = slice_hash(in_msg);
    ;; Receive "withdraw" message
    if (text_op == 0x158e394e7cc73a9aeb362957fe037665588aef16f3bd68580c13ab84097cf22a) {
        throw_unless(59518, ( equal_slices_bits(__tact_context_get_sender(), $self'owner) ));
        $global_send($SendParameters$_constructor_to_bounce_value_mode(__tact_context_get_sender(), true, 0, 130));
        $Treasury$_contract_store(($self'owner, $self'voraTokenAddress, $self'botWalletAddress));
        return ();
    }
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}