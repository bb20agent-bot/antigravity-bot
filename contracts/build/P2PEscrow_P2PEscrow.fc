#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; P2PEscrow_P2PEscrow.headers.fc
;;
;; Header files for P2PEscrow
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_store_address_opt
builder __tact_store_address_opt(builder b, slice address) inline;

;; __tact_not_null
forall X -> X __tact_not_null(X x) impure inline;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_dict_get_int_cell
cell __tact_dict_get_int_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_int_cell
(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline;

;; $DeployOk$_store
builder $DeployOk$_store(builder build_0, (int) v) inline;

;; $DeployOk$_store_cell
cell $DeployOk$_store_cell((int) v, builder b) inline;

;; $TokenTransfer$_store
builder $TokenTransfer$_store(builder build_0, (int, int, slice, slice, cell, int, slice) v) inline;

;; $TokenTransfer$_store_cell
cell $TokenTransfer$_store_cell((int, int, slice, slice, cell, int, slice) v, builder b) inline;

;; $Order$_store
builder $Order$_store(builder build_0, (slice, int, int, int) v) inline;

;; $Order$_store_cell
cell $Order$_store_cell((slice, int, int, int) v, builder b) inline;

;; $Order$_load
(slice, ((slice, int, int, int))) $Order$_load(slice sc_0) inline;

;; $Order$_as_optional
tuple $Order$_as_optional((slice, int, int, int) v) inline;

;; $Order$_load_opt
tuple $Order$_load_opt(cell cl) inline;

;; $P2PEscrow$_store
builder $P2PEscrow$_store(builder build_0, (slice, slice, cell, int) v) inline;

;; $P2PEscrow$_load
(slice, ((slice, slice, cell, int))) $P2PEscrow$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $Order$_not_null
((slice, int, int, int)) $Order$_not_null(tuple v) inline;

;; $P2PEscrow$init$_load
(slice, ((slice))) $P2PEscrow$init$_load(slice sc_0) inline;

;; $P2PEscrow$_contract_init
(slice, slice, cell, int) $P2PEscrow$_contract_init(slice $owner) impure inline;

;; $P2PEscrow$_contract_load
(slice, slice, cell, int) $P2PEscrow$_contract_load() impure inline;

;; $P2PEscrow$_contract_store
() $P2PEscrow$_contract_store((slice, slice, cell, int) v) impure inline;

;; $SendParameters$_constructor_to_value_mode_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline;

;; $TokenTransfer$_constructor_queryId_amount_destination_response_destination_custom_payload_forward_ton_amount_forward_payload
((int, int, slice, slice, cell, int, slice)) $TokenTransfer$_constructor_queryId_amount_destination_response_destination_custom_payload_forward_ton_amount_forward_payload(int $queryId, int $amount, slice $destination, slice $response_destination, cell $custom_payload, int $forward_ton_amount, slice $forward_payload) inline;

;; $P2PEscrow$_fun_sendVora
((slice, slice, cell, int), ()) $P2PEscrow$_fun_sendVora((slice, slice, cell, int) $self, slice $to, int $amount) impure inline_ref;

;; $MessageParameters$_constructor_bounce_to_value_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline;

;; $P2PEscrow$_fun_notify
((slice, slice, cell, int), ()) $P2PEscrow$_fun_notify((slice, slice, cell, int) $self, cell $body) impure inline;

;; $Order$_constructor_seller_amountVora_priceTon_isActive
((slice, int, int, int)) $Order$_constructor_seller_amountVora_priceTon_isActive(slice $seller, int $amountVora, int $priceTon, int $isActive) inline;

;; $SendParameters$_constructor_to_value_mode_bounce_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body(slice $to, int $value, int $mode, int $bounce, cell $body) inline;

;; $DeployOk$_constructor_queryId
((int)) $DeployOk$_constructor_queryId(int $queryId) inline;


;; P2PEscrow_P2PEscrow.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(slice, slice) __tact_load_address_opt(slice cs) asm """

            b{00} SDBEGINSQ
            IF:<{
              PUSHNULL
            }>ELSE<{
              LDMSGADDR
              SWAP
            }>

""";

builder __tact_store_addr_none(builder b) asm "b{00} STSLICECONST";

builder __tact_store_address_opt(builder b, slice address) inline {
    if (null?(address)) {
        return __tact_store_addr_none(b);
    } else {
        return b.store_slice(address);
    }
}

forall X -> X __tact_not_null(X x) impure inline {
    throw_if(128, null?(x)); return x;
}

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

forall X0, X1, X2, X3 -> tuple __tact_tuple_create_4((X0, X1, X2, X3) v) asm """
    4 TUPLE
""";

forall X0, X1, X2, X3 -> (X0, X1, X2, X3) __tact_tuple_destroy_4(tuple v) asm """
    4 UNTUPLE
""";

cell __tact_dict_get_int_cell(cell d, int kl, int k) inline {
    var (r, ok) = idict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_ref(d, kl, k, v), ());
    }
}

slice $global_emptySlice() impure asm """
    b{} PUSHSLICE
""";

int $global_myBalance() impure asm """
    BALANCE FIRST
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

(slice, int) $Slice$_fun_loadCoins(slice $self) impure asm( -> 1 0) """
    LDVARUINT16
""";

((slice, slice, cell, int), ()) $P2PEscrow$_fun_notify((slice, slice, cell, int) $self, cell $body) impure inline {
    var (($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter)) = $self;
    slice $to = __tact_context_get_sender();
    int $bounce = false;
    if (false) {
        int $balance = $global_myBalance();
        int $balanceBeforeMessage = ($balance - $Context$_get_value(__tact_context_get()));
        if (($balanceBeforeMessage < 0)) {
            $global_nativeReserve(0, 0);
            $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 130, $body));
            return (($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter), ());
        }
    }
    $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 66, $body));
    return (($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter), ());
}

;; P2PEscrow_P2PEscrow.constants.fc
;; Cell /txQnnbKgnwLZMVjXURzxddgzTUQFNyifMrsqhm/6nE=
cell __gen_cell_cell_fedc509e76ca827c0b64c5635d4473c5d760cd351014dca27ccaecaa19bfea71() asm """
    B{b5ee9c7241010101001100001e00000000503250205061796d656e74aeb98db8} B>boc PUSHREF
""";

;; P2PEscrow_P2PEscrow.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, true);
}

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_bounce_body(slice $to, int $value, int $mode, int $bounce, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, $bounce);
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: DeployOk
;; Header: 0xaff90f57
;; TLB: deploy_ok#aff90f57 queryId:uint64 = DeployOk
;;

builder $DeployOk$_store(builder build_0, (int) v) inline {
    var (v'queryId) = v;
    build_0 = store_uint(build_0, 2952335191, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    return build_0;
}

cell $DeployOk$_store_cell((int) v, builder b) inline {
    return $DeployOk$_store(b, v).end_cell();
}

((int)) $DeployOk$_constructor_queryId(int $queryId) inline {
    return ($queryId);
}

;;
;; Type: TokenTransfer
;; Header: 0x0f8a7ea5
;; TLB: token_transfer#0f8a7ea5 queryId:uint64 amount:coins destination:address response_destination:address custom_payload:Maybe ^cell forward_ton_amount:coins forward_payload:remainder<slice> = TokenTransfer
;;

builder $TokenTransfer$_store(builder build_0, (int, int, slice, slice, cell, int, slice) v) inline {
    var (v'queryId, v'amount, v'destination, v'response_destination, v'custom_payload, v'forward_ton_amount, v'forward_payload) = v;
    build_0 = store_uint(build_0, 260734629, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    build_0 = build_0.store_varuint16(v'amount);
    build_0 = build_0.store_slice(v'destination);
    build_0 = __tact_store_address_opt(build_0, v'response_destination);
    build_0 = build_0.store_maybe_ref(v'custom_payload);
    build_0 = build_0.store_varuint16(v'forward_ton_amount);
    build_0 = build_0.store_slice(v'forward_payload);
    return build_0;
}

cell $TokenTransfer$_store_cell((int, int, slice, slice, cell, int, slice) v, builder b) inline {
    return $TokenTransfer$_store(b, v).end_cell();
}

((int, int, slice, slice, cell, int, slice)) $TokenTransfer$_constructor_queryId_amount_destination_response_destination_custom_payload_forward_ton_amount_forward_payload(int $queryId, int $amount, slice $destination, slice $response_destination, cell $custom_payload, int $forward_ton_amount, slice $forward_payload) inline {
    return ($queryId, $amount, $destination, $response_destination, $custom_payload, $forward_ton_amount, $forward_payload);
}

;;
;; Type: Order
;; TLB: _ seller:address amountVora:int257 priceTon:int257 isActive:bool = Order
;;

builder $Order$_store(builder build_0, (slice, int, int, int) v) inline {
    var (v'seller, v'amountVora, v'priceTon, v'isActive) = v;
    build_0 = build_0.store_slice(v'seller);
    build_0 = build_0.store_int(v'amountVora, 257);
    build_0 = build_0.store_int(v'priceTon, 257);
    build_0 = build_0.store_int(v'isActive, 1);
    return build_0;
}

cell $Order$_store_cell((slice, int, int, int) v, builder b) inline {
    return $Order$_store(b, v).end_cell();
}

(slice, ((slice, int, int, int))) $Order$_load(slice sc_0) inline {
    var v'seller = sc_0~load_msg_addr();
    var v'amountVora = sc_0~load_int(257);
    var v'priceTon = sc_0~load_int(257);
    var v'isActive = sc_0~load_int(1);
    return (sc_0, (v'seller, v'amountVora, v'priceTon, v'isActive));
}

tuple $Order$_as_optional((slice, int, int, int) v) inline {
    var (v'seller, v'amountVora, v'priceTon, v'isActive) = v;
    return __tact_tuple_create_4(v'seller, v'amountVora, v'priceTon, v'isActive);
}

tuple $Order$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $Order$_as_optional(sc~$Order$_load());
}

((slice, int, int, int)) $Order$_not_null(tuple v) inline {
    throw_if(128, null?(v));
    var (slice vvv'seller, int vvv'amountVora, int vvv'priceTon, int vvv'isActive) = __tact_tuple_destroy_4(v);
    return (vvv'seller, vvv'amountVora, vvv'priceTon, vvv'isActive);
}

((slice, int, int, int)) $Order$_constructor_seller_amountVora_priceTon_isActive(slice $seller, int $amountVora, int $priceTon, int $isActive) inline {
    return ($seller, $amountVora, $priceTon, $isActive);
}

;;
;; Type: P2PEscrow
;; TLB: _ owner:address vora_jetton_wallet:address orders:dict<int, ^Order{seller:address,amountVora:int257,priceTon:int257,isActive:bool}> orderCounter:int257 = P2PEscrow
;;

builder $P2PEscrow$_store(builder build_0, (slice, slice, cell, int) v) inline {
    var (v'owner, v'vora_jetton_wallet, v'orders, v'orderCounter) = v;
    build_0 = build_0.store_slice(v'owner);
    build_0 = __tact_store_address_opt(build_0, v'vora_jetton_wallet);
    build_0 = build_0.store_dict(v'orders);
    build_0 = build_0.store_int(v'orderCounter, 257);
    return build_0;
}

(slice, ((slice, slice, cell, int))) $P2PEscrow$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'vora_jetton_wallet = sc_0~__tact_load_address_opt();
    var v'orders = sc_0~load_dict();
    var v'orderCounter = sc_0~load_int(257);
    return (sc_0, (v'owner, v'vora_jetton_wallet, v'orders, v'orderCounter));
}

(slice, ((slice))) $P2PEscrow$init$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    return (sc_0, (v'owner));
}

(slice, slice, cell, int) $P2PEscrow$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$P2PEscrow$_load();
    }
    else {
        (slice $owner) = $sc~$P2PEscrow$init$_load();
        $sc.end_parse();
        return $P2PEscrow$_contract_init($owner);
    }
}

() $P2PEscrow$_contract_store((slice, slice, cell, int) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $P2PEscrow$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract P2PEscrow functions
;;

(slice, slice, cell, int) $P2PEscrow$_contract_init(slice $owner) impure inline {
    var (($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter)) = (null(), null(), null(), 0);
    $self'owner = $owner;
    return ($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter);
}

((slice, slice, cell, int), ()) $P2PEscrow$_fun_sendVora((slice, slice, cell, int) $self, slice $to, int $amount) impure inline_ref {
    var (($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter)) = $self;
    $global_send($SendParameters$_constructor_to_value_mode_body(__tact_not_null($self'vora_jetton_wallet), 50000000, 2, $TokenTransfer$_store_cell($TokenTransfer$_constructor_queryId_amount_destination_response_destination_custom_payload_forward_ton_amount_forward_payload(0, $amount, $to, null(), null(), 0, $global_emptySlice()), begin_cell())));
    return (($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter), ());
}

;;
;; Get methods of a Contract P2PEscrow
;;

;;
;; Routing of a Contract P2PEscrow
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter) = $P2PEscrow$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    var op = in_msg~load_opcode_internal();
    ;; Receive SetWallet message
    if (op == 1558246425) {
        var $msg'wallet = in_msg~load_msg_addr();
        var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
        throw_unless(14534, ( equal_slices_bits($ctx'sender, $self'owner) ));
        $self'vora_jetton_wallet = $msg'wallet;
        $P2PEscrow$_contract_store(($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter));
        return ();
    }
    
    ;; Receive TokenNotification message
    if (op == 0x7362d09c) {
        var $msg'queryId = in_msg~load_uint(64);
        var $msg'amount = in_msg~load_varuint16();
        var $msg'from = in_msg~load_msg_addr();
        var $msg'forward_payload = in_msg;
        var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
        throw_unless(59007, ( ((~ null?($self'vora_jetton_wallet))) ? (( equal_slices_bits($ctx'sender, __tact_not_null($self'vora_jetton_wallet)) )) : (false) ));
        slice $slice = $msg'forward_payload;
        int $op = $slice~load_uint(32);
        if (($op == 1)) {
            int $priceTon = $slice~$Slice$_fun_loadCoins();
            $self'orderCounter = ($self'orderCounter + 1);
            $self'orders~__tact_dict_set_int_cell(257, $self'orderCounter, $Order$_store_cell($Order$_constructor_seller_amountVora_priceTon_isActive($msg'from, $msg'amount, $priceTon, true), begin_cell()));
        } else {
            ($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter)~$P2PEscrow$_fun_sendVora($msg'from, $msg'amount);
        }
        $P2PEscrow$_contract_store(($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter));
        return ();
    }
    
    ;; Receive FillOrder message
    if (op == 2174048283) {
        var $msg'orderId = in_msg~load_int(257);
        var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
        tuple $order = $Order$_load_opt(__tact_dict_get_int_cell($self'orders, 257, $msg'orderId));
        throw_unless(18518, (~ null?($order)));
        var ($o'seller, $o'amountVora, $o'priceTon, $o'isActive) = $Order$_not_null($order);
        throw_unless(62658, $o'isActive);
        throw_unless(48341, ($ctx'value >= $o'priceTon));
        $global_send($SendParameters$_constructor_to_value_mode_bounce_body($o'seller, $o'priceTon, 2, false, __gen_cell_cell_fedc509e76ca827c0b64c5635d4473c5d760cd351014dca27ccaecaa19bfea71()));
        ($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter)~$P2PEscrow$_fun_sendVora($ctx'sender, $o'amountVora);
        $self'orders~__tact_dict_set_int_cell(257, $msg'orderId, $Order$_store_cell($Order$_constructor_seller_amountVora_priceTon_isActive($o'seller, $o'amountVora, $o'priceTon, false), begin_cell()));
        $P2PEscrow$_contract_store(($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter));
        return ();
    }
    
    ;; Receive CancelOrder message
    if (op == 2170307283) {
        var $msg'orderId = in_msg~load_int(257);
        var ($ctx'bounceable, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
        tuple $order = $Order$_load_opt(__tact_dict_get_int_cell($self'orders, 257, $msg'orderId));
        throw_unless(18518, (~ null?($order)));
        var ($o'seller, $o'amountVora, $o'priceTon, $o'isActive) = $Order$_not_null($order);
        throw_unless(62658, $o'isActive);
        throw_unless(42435, ( (( equal_slices_bits($ctx'sender, $o'seller) )) ? (true) : (( equal_slices_bits($ctx'sender, $self'owner) )) ));
        ($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter)~$P2PEscrow$_fun_sendVora($o'seller, $o'amountVora);
        $self'orders~__tact_dict_set_int_cell(257, $msg'orderId, $Order$_store_cell($Order$_constructor_seller_amountVora_priceTon_isActive($o'seller, $o'amountVora, $o'priceTon, false), begin_cell()));
        $P2PEscrow$_contract_store(($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter));
        return ();
    }
    
    ;; Receive Deploy message
    if (op == 0x946a98b6) {
        var $deploy'queryId = in_msg~load_uint(64);
        ($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter)~$P2PEscrow$_fun_notify($DeployOk$_store_cell($DeployOk$_constructor_queryId($deploy'queryId), begin_cell()));
        $P2PEscrow$_contract_store(($self'owner, $self'vora_jetton_wallet, $self'orders, $self'orderCounter));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}