#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; VestingVault_VestingVault.headers.fc
;;
;; Header files for VestingVault
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_dict_get_slice_cell
cell __tact_dict_get_slice_cell(cell d, int kl, slice k) inline;

;; __tact_dict_set_slice_cell
(cell, ()) __tact_dict_set_slice_cell(cell d, int kl, slice k, cell v) inline;

;; $DeployOk$_store
builder $DeployOk$_store(builder build_0, (int) v) inline;

;; $DeployOk$_store_cell
cell $DeployOk$_store_cell((int) v, builder b) inline;

;; $VestingSchedule$_store
builder $VestingSchedule$_store(builder build_0, (slice, int, int, int, int, int) v) inline;

;; $VestingSchedule$_store_cell
cell $VestingSchedule$_store_cell((slice, int, int, int, int, int) v, builder b) inline;

;; $VestingSchedule$_load
(slice, ((slice, int, int, int, int, int))) $VestingSchedule$_load(slice sc_0) inline;

;; $VestingSchedule$_as_optional
tuple $VestingSchedule$_as_optional((slice, int, int, int, int, int) v) inline;

;; $VestingSchedule$_load_opt
tuple $VestingSchedule$_load_opt(cell cl) inline;

;; $VestingVault$_store
builder $VestingVault$_store(builder build_0, (slice, slice, cell) v) inline;

;; $VestingVault$_load
(slice, ((slice, slice, cell))) $VestingVault$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $VestingSchedule$_not_null
((slice, int, int, int, int, int)) $VestingSchedule$_not_null(tuple v) inline;

;; $VestingVault$init$_load
(slice, ((slice, slice))) $VestingVault$init$_load(slice sc_0) inline;

;; $VestingVault$_contract_init
(slice, slice, cell) $VestingVault$_contract_init(slice $owner, slice $voraTokenAddress) impure inline;

;; $VestingVault$_contract_load
(slice, slice, cell) $VestingVault$_contract_load() impure inline;

;; $VestingVault$_contract_store
() $VestingVault$_contract_store((slice, slice, cell) v) impure inline;

;; $VestingVault$_fun_calculateVestedAmount
((slice, slice, cell), int) $VestingVault$_fun_calculateVestedAmount((slice, slice, cell) $self, (slice, int, int, int, int, int) $schedule) impure inline_ref;

;; $MessageParameters$_constructor_bounce_to_value_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline;

;; $VestingVault$_fun_notify
((slice, slice, cell), ()) $VestingVault$_fun_notify((slice, slice, cell) $self, cell $body) impure inline;

;; $VestingSchedule$_constructor_beneficiary_totalAmount_releasedAmount_cliffEnd_linearEnd_unlockMultiplier
((slice, int, int, int, int, int)) $VestingSchedule$_constructor_beneficiary_totalAmount_releasedAmount_cliffEnd_linearEnd_unlockMultiplier(slice $beneficiary, int $totalAmount, int $releasedAmount, int $cliffEnd, int $linearEnd, int $unlockMultiplier) inline;

;; $DeployOk$_constructor_queryId
((int)) $DeployOk$_constructor_queryId(int $queryId) inline;


;; VestingVault_VestingVault.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(cell, int) __tact_dict_delete(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTDEL
""";

((cell), ()) __tact_dict_set_ref(cell dict, int key_len, slice index, cell value) asm(value index dict key_len) """
    DICTSETREF
""";

(cell, int) __tact_dict_get_ref(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTGETREF NULLSWAPIFNOT
""";

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

forall X0, X1, X2, X3, X4, X5 -> tuple __tact_tuple_create_6((X0, X1, X2, X3, X4, X5) v) asm """
    6 TUPLE
""";

forall X0, X1, X2, X3, X4, X5 -> (X0, X1, X2, X3, X4, X5) __tact_tuple_destroy_6(tuple v) asm """
    6 UNTUPLE
""";

cell __tact_dict_get_slice_cell(cell d, int kl, slice k) inline {
    var (r, ok) = __tact_dict_get_ref(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_slice_cell(cell d, int kl, slice k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = __tact_dict_delete(d, kl, k);
        return (r, ());
    } else {
        return __tact_dict_set_ref(d, kl, k, v);
    }
}

int $global_myBalance() impure asm """
    BALANCE FIRST
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

int $global_now() impure asm """
    NOW
""";

((slice, slice, cell), ()) $VestingVault$_fun_notify((slice, slice, cell) $self, cell $body) impure inline {
    var (($self'owner, $self'voraTokenAddress, $self'schedules)) = $self;
    slice $to = __tact_context_get_sender();
    int $bounce = false;
    if (false) {
        int $balance = $global_myBalance();
        int $balanceBeforeMessage = ($balance - $Context$_get_value(__tact_context_get()));
        if (($balanceBeforeMessage < 0)) {
            $global_nativeReserve(0, 0);
            $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 130, $body));
            return (($self'owner, $self'voraTokenAddress, $self'schedules), ());
        }
    }
    $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 66, $body));
    return (($self'owner, $self'voraTokenAddress, $self'schedules), ());
}

;; VestingVault_VestingVault.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: DeployOk
;; Header: 0xaff90f57
;; TLB: deploy_ok#aff90f57 queryId:uint64 = DeployOk
;;

builder $DeployOk$_store(builder build_0, (int) v) inline {
    var (v'queryId) = v;
    build_0 = store_uint(build_0, 2952335191, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    return build_0;
}

cell $DeployOk$_store_cell((int) v, builder b) inline {
    return $DeployOk$_store(b, v).end_cell();
}

((int)) $DeployOk$_constructor_queryId(int $queryId) inline {
    return ($queryId);
}

;;
;; Type: VestingSchedule
;; TLB: _ beneficiary:address totalAmount:int257 releasedAmount:int257 cliffEnd:int257 linearEnd:int257 unlockMultiplier:int257 = VestingSchedule
;;

builder $VestingSchedule$_store(builder build_0, (slice, int, int, int, int, int) v) inline {
    var (v'beneficiary, v'totalAmount, v'releasedAmount, v'cliffEnd, v'linearEnd, v'unlockMultiplier) = v;
    build_0 = build_0.store_slice(v'beneficiary);
    build_0 = build_0.store_int(v'totalAmount, 257);
    build_0 = build_0.store_int(v'releasedAmount, 257);
    var build_1 = begin_cell();
    build_1 = build_1.store_int(v'cliffEnd, 257);
    build_1 = build_1.store_int(v'linearEnd, 257);
    build_1 = build_1.store_int(v'unlockMultiplier, 257);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

cell $VestingSchedule$_store_cell((slice, int, int, int, int, int) v, builder b) inline {
    return $VestingSchedule$_store(b, v).end_cell();
}

(slice, ((slice, int, int, int, int, int))) $VestingSchedule$_load(slice sc_0) inline {
    var v'beneficiary = sc_0~load_msg_addr();
    var v'totalAmount = sc_0~load_int(257);
    var v'releasedAmount = sc_0~load_int(257);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'cliffEnd = sc_1~load_int(257);
    var v'linearEnd = sc_1~load_int(257);
    var v'unlockMultiplier = sc_1~load_int(257);
    return (sc_0, (v'beneficiary, v'totalAmount, v'releasedAmount, v'cliffEnd, v'linearEnd, v'unlockMultiplier));
}

tuple $VestingSchedule$_as_optional((slice, int, int, int, int, int) v) inline {
    var (v'beneficiary, v'totalAmount, v'releasedAmount, v'cliffEnd, v'linearEnd, v'unlockMultiplier) = v;
    return __tact_tuple_create_6(v'beneficiary, v'totalAmount, v'releasedAmount, v'cliffEnd, v'linearEnd, v'unlockMultiplier);
}

tuple $VestingSchedule$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $VestingSchedule$_as_optional(sc~$VestingSchedule$_load());
}

((slice, int, int, int, int, int)) $VestingSchedule$_tensor_cast((slice, int, int, int, int, int) v) asm "NOP";

((slice, int, int, int, int, int)) $VestingSchedule$_not_null(tuple v) inline {
    throw_if(128, null?(v));
    var (slice vvv'beneficiary, int vvv'totalAmount, int vvv'releasedAmount, int vvv'cliffEnd, int vvv'linearEnd, int vvv'unlockMultiplier) = __tact_tuple_destroy_6(v);
    return (vvv'beneficiary, vvv'totalAmount, vvv'releasedAmount, vvv'cliffEnd, vvv'linearEnd, vvv'unlockMultiplier);
}

((slice, int, int, int, int, int)) $VestingSchedule$_constructor_beneficiary_totalAmount_releasedAmount_cliffEnd_linearEnd_unlockMultiplier(slice $beneficiary, int $totalAmount, int $releasedAmount, int $cliffEnd, int $linearEnd, int $unlockMultiplier) inline {
    return ($beneficiary, $totalAmount, $releasedAmount, $cliffEnd, $linearEnd, $unlockMultiplier);
}

;;
;; Type: VestingVault
;; TLB: _ owner:address voraTokenAddress:address schedules:dict<address, ^VestingSchedule{beneficiary:address,totalAmount:int257,releasedAmount:int257,cliffEnd:int257,linearEnd:int257,unlockMultiplier:int257}> = VestingVault
;;

builder $VestingVault$_store(builder build_0, (slice, slice, cell) v) inline {
    var (v'owner, v'voraTokenAddress, v'schedules) = v;
    build_0 = build_0.store_slice(v'owner);
    build_0 = build_0.store_slice(v'voraTokenAddress);
    build_0 = build_0.store_dict(v'schedules);
    return build_0;
}

(slice, ((slice, slice, cell))) $VestingVault$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'voraTokenAddress = sc_0~load_msg_addr();
    var v'schedules = sc_0~load_dict();
    return (sc_0, (v'owner, v'voraTokenAddress, v'schedules));
}

(slice, ((slice, slice))) $VestingVault$init$_load(slice sc_0) inline {
    var v'owner = sc_0~load_msg_addr();
    var v'voraTokenAddress = sc_0~load_msg_addr();
    return (sc_0, (v'owner, v'voraTokenAddress));
}

(slice, slice, cell) $VestingVault$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$VestingVault$_load();
    }
    else {
        (slice $owner, slice $voraTokenAddress) = $sc~$VestingVault$init$_load();
        $sc.end_parse();
        return $VestingVault$_contract_init($owner, $voraTokenAddress);
    }
}

() $VestingVault$_contract_store((slice, slice, cell) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $VestingVault$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract VestingVault functions
;;

(slice, slice, cell) $VestingVault$_contract_init(slice $owner, slice $voraTokenAddress) impure inline {
    var (($self'owner, $self'voraTokenAddress, $self'schedules)) = (null(), null(), null());
    $self'owner = $owner;
    $self'voraTokenAddress = $voraTokenAddress;
    return ($self'owner, $self'voraTokenAddress, $self'schedules);
}

((slice, slice, cell), int) $VestingVault$_fun_calculateVestedAmount((slice, slice, cell) $self, (slice, int, int, int, int, int) $schedule) impure inline_ref {
    var (($self'owner, $self'voraTokenAddress, $self'schedules)) = $self;
    var (($schedule'beneficiary, $schedule'totalAmount, $schedule'releasedAmount, $schedule'cliffEnd, $schedule'linearEnd, $schedule'unlockMultiplier)) = $schedule;
    if (($global_now() < $schedule'cliffEnd)) {
        var $fresh$ret_9 = 0;
        return (($self'owner, $self'voraTokenAddress, $self'schedules), $fresh$ret_9);
    } elseif (($global_now() >= $schedule'linearEnd)) {
        var $fresh$ret_10 = $schedule'totalAmount;
        return (($self'owner, $self'voraTokenAddress, $self'schedules), $fresh$ret_10);
    } else {
        int $passed = ($global_now() - $schedule'cliffEnd);
        int $totalDuration = ($schedule'linearEnd - $schedule'cliffEnd);
        int $baseVested = (($schedule'totalAmount * $passed) / $totalDuration);
        var $fresh$ret_11 = (($baseVested * $schedule'unlockMultiplier) / 100);
        return (($self'owner, $self'voraTokenAddress, $self'schedules), $fresh$ret_11);
    }
}

;;
;; Get methods of a Contract VestingVault
;;

;;
;; Routing of a Contract VestingVault
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'owner, $self'voraTokenAddress, $self'schedules) = $VestingVault$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    var op = in_msg~load_opcode_internal();
    ;; Receive AddBeneficiary message
    if (op == 2646524585) {
        var $msg'beneficiary = in_msg~load_msg_addr();
        var $msg'amount = in_msg~load_int(257);
        var $msg'cliffDuration = in_msg~load_int(257);
        slice sc_1 = in_msg~load_ref().begin_parse();
        var $msg'linearDuration = sc_1~load_int(257);
        throw_unless(55063, ( equal_slices_bits(__tact_context_get_sender(), $self'owner) ));
        throw_unless(15970, null?($VestingSchedule$_load_opt(__tact_dict_get_slice_cell($self'schedules, 267, $msg'beneficiary))));
        int $currentTime = $global_now();
        $self'schedules~__tact_dict_set_slice_cell(267, $msg'beneficiary, $VestingSchedule$_store_cell($VestingSchedule$_constructor_beneficiary_totalAmount_releasedAmount_cliffEnd_linearEnd_unlockMultiplier($msg'beneficiary, $msg'amount, 0, ($currentTime + $msg'cliffDuration), (($currentTime + $msg'cliffDuration) + $msg'linearDuration), 100), begin_cell()));
        $VestingVault$_contract_store(($self'owner, $self'voraTokenAddress, $self'schedules));
        return ();
    }
    
    ;; Receive ReleaseTokens message
    if (op == 2959791690) {
        var $msg'beneficiary = in_msg~load_msg_addr();
        var ($schedule'beneficiary, $schedule'totalAmount, $schedule'releasedAmount, $schedule'cliffEnd, $schedule'linearEnd, $schedule'unlockMultiplier) = $VestingSchedule$_not_null($VestingSchedule$_load_opt(__tact_dict_get_slice_cell($self'schedules, 267, $msg'beneficiary)));
        throw_unless(5346, ($schedule'totalAmount > 0));
        int $vestedAmount = ($self'owner, $self'voraTokenAddress, $self'schedules)~$VestingVault$_fun_calculateVestedAmount($VestingSchedule$_tensor_cast(($schedule'beneficiary, $schedule'totalAmount, $schedule'releasedAmount, $schedule'cliffEnd, $schedule'linearEnd, $schedule'unlockMultiplier)));
        int $unreleased = ($vestedAmount - $schedule'releasedAmount);
        throw_unless(18958, ($unreleased > 0));
        $schedule'releasedAmount = ($schedule'releasedAmount + $unreleased);
        $self'schedules~__tact_dict_set_slice_cell(267, $msg'beneficiary, $VestingSchedule$_store_cell(($schedule'beneficiary, $schedule'totalAmount, $schedule'releasedAmount, $schedule'cliffEnd, $schedule'linearEnd, $schedule'unlockMultiplier), begin_cell()));
        $VestingVault$_contract_store(($self'owner, $self'voraTokenAddress, $self'schedules));
        return ();
    }
    
    ;; Receive AdjustMultiplier message
    if (op == 3866411903) {
        var $msg'beneficiary = in_msg~load_msg_addr();
        var $msg'newMultiplier = in_msg~load_int(257);
        throw_unless(29958, ( equal_slices_bits(__tact_context_get_sender(), $self'owner) ));
        var ($schedule'beneficiary, $schedule'totalAmount, $schedule'releasedAmount, $schedule'cliffEnd, $schedule'linearEnd, $schedule'unlockMultiplier) = $VestingSchedule$_not_null($VestingSchedule$_load_opt(__tact_dict_get_slice_cell($self'schedules, 267, $msg'beneficiary)));
        throw_unless(5346, ($schedule'totalAmount > 0));
        $schedule'unlockMultiplier = $msg'newMultiplier;
        $self'schedules~__tact_dict_set_slice_cell(267, $msg'beneficiary, $VestingSchedule$_store_cell(($schedule'beneficiary, $schedule'totalAmount, $schedule'releasedAmount, $schedule'cliffEnd, $schedule'linearEnd, $schedule'unlockMultiplier), begin_cell()));
        $VestingVault$_contract_store(($self'owner, $self'voraTokenAddress, $self'schedules));
        return ();
    }
    
    ;; Receive Deploy message
    if (op == 0x946a98b6) {
        var $deploy'queryId = in_msg~load_uint(64);
        ($self'owner, $self'voraTokenAddress, $self'schedules)~$VestingVault$_fun_notify($DeployOk$_store_cell($DeployOk$_constructor_queryId($deploy'queryId), begin_cell()));
        $VestingVault$_contract_store(($self'owner, $self'voraTokenAddress, $self'schedules));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}